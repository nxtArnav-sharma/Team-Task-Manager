{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo 'Running migrations...' && npx prisma migrate deploy && echo 'Starting server...' && node server.js",
    "healthcheckPath": "/login",
    "restartPolicyType": "ON_FAILURE"
  }
}
